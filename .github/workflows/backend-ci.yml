name: Backend CI

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle/**"
      - "config/**"
      - ".editorconfig"
      - ".github/workflows/backend-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "src/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle/**"
      - "config/**"
      - ".editorconfig"
      - ".github/workflows/backend-ci.yml"

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Initialize Gradle Wrapper
        run: gradle wrapper

      - name: Run Checkstyle Main (메인 소스 코드 스타일 검사)
        run: ./gradlew checkstyleMain --no-daemon

      - name: Run Checkstyle Test (테스트 소스 코드 스타일 검사)
        run: ./gradlew checkstyleTest --no-daemon

      - name: Run Tests (테스트 실행)
        run: ./gradlew test --no-daemon

      - name: Build with Gradle (빌드만 실행)
        run: ./gradlew build -x test -x checkstyleMain -x checkstyleTest --no-daemon

      - name: Upload Checkstyle report
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: checkstyle-report
          path: build/reports/checkstyle/**/*

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: test-results
          path: build/test-results/test/**/*.xml

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: test-report
          path: build/reports/tests/test/**/*
